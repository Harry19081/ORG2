# 托管模型服务客户端交接

2026-09-16。实现待验收。用户明确要求先盲实现、之后集中测试，因此本轮没有执行自动化测试、类型检查、Rust 编译、桌面构建、Computer Use 或原生应用验证。格式化完成不等于可编译或功能通过。

## 已提交的客户端链路

- 从公开 #1846 的固定 head `d6643ab39059a7cc89f3381529c7562f57c65c63` 选择迁入原生授权、OS 凭据存储、动态认证、会话代理、执行 profile、配置备份/冲突/恢复和相应历史路径依赖，没有整体合并旧分支。引用的测试与 fixture 也随相关调用链保留，但本次未运行。
- `src-tauri/crates/market-connect/src/services.rs` 读取带 `schema_version: 1` 的目录、确认用量授权并按 access/model/session 获取短时认证。目录分页每页 50、总数上限 100，响应字节有限制；未知协议、无效标识和越界响应拒绝接入。
- `workspace.rs` 将托管目录投影到现有 selector 合同；新增 `managed` 为可选字段，旧工作区 wire 路径仍可解析。已保存旧来源引用保留严格校验，不把缺失授权自动改成个人账号。
- `source.rs` 把授权连接、稳定 access、所选模型和会话标识放入无秘密来源引用，贯穿 Tauri → 正常会话创建/继续 → 持久化 → 重新打开。本地代理按需获得 bearer；renderer、IPC、配置文件只得到来源引用或本地代理凭据。
- 首次连接可从 Console 发起无需已有工作区的 ORG2 授权。新流程使用明确的初始连接 marker，原工作区链接仍按既有字段校验；不能用旧客户端启动了 URL 作为已兼容新目录的证据。
- 模型选择器使用动态目录。首次选模型或服务端要求重新确认时，`UsageAuthorizationHost` 展示买家单价、计费维度和总使用上限；明确确认后才申请启用。预算不足、授权失效等由服务器裁决，客户端不推算业务规则。
- `AppConnectionPage` 为每个 App 独立选择服务和模型。删除了聊天模型选择时自动重配/恢复其他 App 的逻辑；用户对一个 App 的操作只作用于该 App。
- 原生配置继续使用已有 hash 冲突保护、备份与恢复。Claude Code 打开终端，Claude Desktop/Codex 走对应的原生启动入口；ORG2 必须运行才能为动态来源提供本地代理。部分启动入口有平台限制，不能据此声明跨平台支持。
- Market 专用 MCP 注入/路由未迁入，正常用户 MCP/Skill 仍走本地执行链。
- 目录缓存 30 秒、连接变化失效、并发读取合并，过期请求不能覆盖更新后的读取。原生连接缓存最多 32 个 owner，每个 owner 最多 32 个短时认证，按过期时间淘汰；已结束会话不会永久消耗来源选择名额。运行中的 session route 另有独立拥有权与释放路径。

## 公开协议与兼容范围

| 调用 | 客户端读取/提交 |
| --- | --- |
| `GET /v1/market/packages` | schema、服务 ID/名称/版本、模型 ID/协议/兼容客户端、买家报价、公开可用状态、现有 access |
| `POST /v1/market/package-access` | 服务与模型、预期版本/revision、总预算、明确确认；返回无秘密 access |
| `POST /v1/market/package-access/:id/token` | 精确模型与会话；原生模块校验返回的 access、workspace、模型、session、有效期和 gateway origin |
| Tauri `market_connection_activate_service` | renderer 发起明确授权操作；原生层读取当前授权连接并代理公开 API |
| Tauri prepare/configure | 返回不含 bearer 的来源引用，分别用于单会话和单 App 的配置 |

新来源保持 `market:` 引用格式，含经过严格校验的选择信息；新增 model/session 字段仅对新 access 必填。服务器目录和短时授权响应显式检查版本。公开客户端不内置商品列表或服务器端价格计算策略。

兼容范围按服务器公开能力决定。首轮验收覆盖现有 Messages 与 Responses 执行器的文本、客户端工具和流式请求；服务端历史对象、多模态、其他协议或原生应用特殊请求不应推断为已支持。

## 交给下一轮模型的验收顺序

先记录本分支 commit、应用 bundle ID、实际启动进程、客户端版本及所连接的测试环境。必须构建此分支，不能拿旧安装包、旧 PR 截图或仅网页结果替代本次验收。

1. 先运行本仓编译与相关测试，处理类型/平台差异；随后以真实构建进行 Computer Use。记录执行命令、结果和失败原因。本轮未新增任何测试通过声明。
2. 全新用户无已有服务记录：打开模型选择器/连接入口，授权、返回、读取真实目录，取消和重试，空/加载/失效/未知版本状态。
3. 分别选择不同协议的服务/模型，确认真实展示的单价与总用量上限，完成启用。余额不足时访问钱包，回到原选择重试；更高价格或新模型要求重新确认；取消授权不能启动请求。
4. ORG2 正常新建会话、文本往返、本地工具多轮、流式取消、继续、保存、重新打开、完整退出再启动。检查来源和模型保留、不回退到个人账号；切换新会话不改写旧会话。
5. 分别配置 Claude Code、Claude Desktop、Codex：选服务、选模型、启动、真实调用、关闭、再次打开。每个目标单独记录，不把一次成功外推到其他目标或平台。
6. A App 切服务/模型，不改 B App；ORG2 聊天中切托管模型或个人账号，不改已连接 App。断开只恢复所选 App。外部修改配置后，连接/恢复操作准确显示冲突并保留外部修改。
7. 区分仅关窗口与全量退出；确认代理/锁/执行 profile 生命周期，重启后重建动态认证。再测多实例互斥与授权撤销时正在执行的请求。
8. 连续创建并结束超过 32 个会话，检查 token 缓存淘汰、路由释放和历史恢复；重复打开/关闭选择器，观察并发读取、空闲网络、CPU/RAM 与订阅数量。
9. 个人 Key Vault、正常本地 MCP/Skill 和历史导入路径回归。旧来源可解析，但旧托管来源在新目录中的恢复/配置体验仍需实际确认。
10. 亮/暗主题、中/英文、窄窗口和加载/空/错误/不可用状态，保存截图。检查浏览器/renderer、IPC、日志及配置文件不出现上游凭据或长期授权。

## 架构交接记录

| 层 | 本轮源码处理与尚缺证据 |
| --- | --- |
| 1 编译正确性 | 按用户要求未运行编译/类型检查，待下一轮 |
| 2 依赖/重复路径 | 沿授权→source→session/proxy→原生配置追踪；移除本功能不需要的 Market MCP 与跨 App 自动同步 |
| 3 命名 | UI 使用官方 Package/托管服务；`WorkspaceEntitlement` 等兼容 wire 名保留，新增 managed 数据单独表达 |
| 4 语义 | 无秘密来源引用与认证分离；用量授权与原生配置是不同操作 |
| 5 默认分支 | 未知协议、无授权、不支持模型拒绝，不切换个人账号作为后备 |
| 6 职责 | 通用动态凭据/会话路由不包含服务端业务策略 |
| 7 可读性 | 在此列出公开调用、迁入依赖和执行路径，不依赖会话上下文理解修改 |
| 8 Wire | Rust/TypeScript 同批新增目录与 access 结构；序列化与未知版本实测待执行 |
| 9 初始化 | 创建/运行/继续及测试入口传递 credential source；各入口实测待执行 |
| 10 恢复 | session 持久化、历史投影、执行 profile 恢复均传递来源；进程重启证据待补 |

依赖与锁文件变化用于编入原生连接模块。回退客户端前先断开/恢复它管理的外部 App 配置，并保留配置备份与用户历史；不要通过删除 OS 凭据或历史来模拟成功回滚。没有执行部署、用户配置改写或实际连接。
